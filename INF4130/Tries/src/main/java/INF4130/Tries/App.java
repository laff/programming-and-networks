package INF4130.Tries;

/**
 * Hello world!
 *
 */
public class App {
    public static void main( String[] args ) {
    	
    	Tries tries = new Tries();
    	
    	tries.init();
    	
    	/*
    	// I want two arguments only
    	// name of input file first, then output 
    	int len = args.length;
    	if (len == 2) {
    		tries.setInput(args[0]);
    		tries.setOutput(args[1]);
    		
        	// Initiate program!
        	tries.init();
    	}
    	*/
    } 
}
